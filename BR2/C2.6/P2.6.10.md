# СПЕЦИФИКАЦИЯ ПАТЧА P2.6.10 (L5)

**ID:** P2.6.10  
**Контейнер:** C2.6 (Project Memory)  
**Название:** Обновление README и healthcheck

## 1. Цель
Обновить `README.md` с описанием переменных окружения, API, инструкцией по запуску. Убедиться, что healthcheck настроен в `docker-compose.yml`.

## 2. Технические параметры
- **Файл:** `BR2/C2.6/README.md`
- **Содержание:** название, описание, переменные (`PORT`, `BR18_URL`, `PROJECTS_ROOT`, `CHROMA_PATH`), примеры запросов.
- **Healthcheck:** проверить, что в `docker-compose.yml` для C2.6 есть блок `healthcheck`. Если нет – добавить аналогично другим сервисам (эндпоинт `/health`).

## 3. Критерии приёмки (DoD)
- [ ] README содержит актуальную информацию.
- [ ] Healthcheck настроен и работает (проверка через `docker ps`).